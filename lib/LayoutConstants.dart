import 'package:pixi/pixi.dart';

class LayoutConstants {
  static num get DayWidth => 20;
  static num get HorizontalMargin => 16;
  static num get TitleWidth => 100;
  static Colour get Foreground => new Colour(200, 200, 200);
}